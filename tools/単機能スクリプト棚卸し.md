# tools/ 単機能スクリプト棚卸し（統合ツール対応）

統合方針: `tools/統合ツール/src/tabs/` を**正規実装**とし、`tools/*.js` は**単機能スタンドアロン実行スクリプト**として管理する。

**差分比較**のみ `bundleEntry: diff-lite-entry.js` で esbuild し、差分＋出力を単一スクリプトに同梱（`統合ツール.js` 不要）。それ以外の単機能スクリプトは**薄いラッパー**のみで、起動時に `統合ツール.js` を読み込み、**該当タブだけ**を開く（全タブの実装は `統合ツール.js` 1 本に集約され、個別ファイルに全機能を重複同梱しない）。

## マッピング表

| 単機能スクリプト | 統合側モジュール | 判定 | 対応方針 |
|---|---|---|---|
| `差分比較.js` | `tabs/diff.js` | 軽量 esbuild 同梱（`diff-lite-ui.js` 等・`統合ツール.js` 不要） | ビルドで自動生成 |
| `プレビュー反映.js` | `tabs/reflect.js` | 薄いラッパー（`統合ツール.js` 読み込み） | ビルドで自動生成 |
| `フィールド追加.js` | `tabs/field.js` | 軽量 esbuild 同梱（`src/entries/field-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `kintoneJS取得.js` | `tabs/jsconfig.js` | 軽量 esbuild 同梱（`src/entries/jsconfig-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `設定取得.js` | `tabs/settings-export.js` | 軽量 esbuild 同梱（`src/entries/settings-export-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `設計書作成.js` | `tabs/design.js` | 軽量 esbuild 同梱（`src/entries/design-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `ER図.js` | `tabs/er.js` | 軽量 esbuild 同梱（`src/entries/er-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `プロセス実行.js` | `tabs/process.js` | 軽量 esbuild 同梱（`src/entries/process-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `kintoneレコード取得.js` | `tabs/record.js` | 軽量 esbuild 同梱（`src/entries/record-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |
| `kintoneSQL.js` | `tabs/sql.js` | 軽量 esbuild 同梱（`src/entries/sql-lite-entry.js`・`統合ツール.js` 不要） | ビルドで自動生成 |

## 運用ルール

- 単機能スクリプトはすべて `npm run build` で再生成する（エントリ一覧は `tools/統合ツール/src/featureDefs.mjs` の `STANDALONE_LAUNCH_ENTRIES`）。
- `STANDALONE_LAUNCH_ENTRIES` の各項目は `bundleEntry` で esbuild される。`window.__KUS__.runDiffStandalone` は `register-api.js`（統合版・フル単機バンドル双方に含まれる場合あり）で公開。
- `tools/*.js` は手編集せず、`tools/統合ツール/src/tabs/*.js` 側を修正する。
- 単体で実行しても対象機能へ直接遷移することを前提とする。

## 機能一覧表

| 機能名 | 正規モジュール | 単機能スクリプト |
|---|---|---|
| 差分比較 | `tools/統合ツール/src/tabs/diff.js` | `tools/差分比較.js` |
| プレビュー反映 | `tools/統合ツール/src/tabs/reflect.js` | `tools/プレビュー反映.js` |
| フィールド追加 | `tools/統合ツール/src/tabs/field.js` | `tools/フィールド追加.js` |
| JS/CSS設定 | `tools/統合ツール/src/tabs/jsconfig.js` | `tools/kintoneJS取得.js` |
| 設定一括取得 | `tools/統合ツール/src/tabs/settings-export.js` | `tools/設定取得.js` |
| 設計書 | `tools/統合ツール/src/tabs/design.js` | `tools/設計書作成.js` |
| ER図 | `tools/統合ツール/src/tabs/er.js` | `tools/ER図.js` |
| プロセス図 | `tools/統合ツール/src/tabs/process.js` | `tools/プロセス実行.js` |
| レコード管理 | `tools/統合ツール/src/tabs/record.js` | `tools/kintoneレコード取得.js` |
| SQL実行 | `tools/統合ツール/src/tabs/sql.js` | `tools/kintoneSQL.js` |
